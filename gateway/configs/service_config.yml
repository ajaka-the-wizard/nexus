services:
  AUTH_SERVICE:
    host: "localhost"
    port: 5000
    protocol: "http"
    rate_limiting_cost: 10
  USER_SERVICE:
    host: "localhost"
    port: 6000
    protocol: "http"
    rate_limiting_cost: 8
  ORDER_SERVICE:
    host: "localhost"
    port: 7000
    protocol: "http"
    rate_limiting_cost: 6
  INVENTORY_SERVICE:
    host: "localhost"
    port: 8000
    protocol: "http"
    rate_limiting_cost: 5